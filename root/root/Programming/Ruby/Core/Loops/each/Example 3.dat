arr = %w[q w e r t]
puts arr.map.each(&:upcase)
#=> ["Q", "W", "E", "R", "T", "Y"]

arr = [1, 2, 3]
puts arr.map.each(&:next)

=begin
    `map` could be replaced by `to_enum`.
    One needs to convert the array into an Enumerator to be able to use the block-symbol-method thingy.
=end